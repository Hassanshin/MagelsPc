using Unity.Entities;
using Unity.Mathematics;
using UnityEngine;

namespace Baker
{
    public class #SCRIPTNAME# : MonoBehaviour
    {
        public #SCRIPTNAME#Component Comp = new()
        {
            Value = 1,
        };
    }

    public class #SCRIPTNAME#Baker : Baker<#SCRIPTNAME#>
    {
        public override void Bake(#SCRIPTNAME# authoring)
        {
            Entity entity = GetEntity(authoring, TransformUsageFlags.None);
            
            AddComponent(entity, authoring.Comp);
        }
    }
}

#ROOTNAMESPACEBEGIN#
	[System.Serializable]
    public struct #SCRIPTNAME#Component : IComponentData
    {
        public float Value;
    }
#ROOTNAMESPACEEND#
